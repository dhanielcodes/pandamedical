// var httpStatus = require('http-status');

const OrgResponse = {
    status: 200,
    response: true,
    data: null,
    message: null,
    errMessage: null,
}

const Response = { ...OrgResponse };

module.exports = Response;
